# A CONFIDENTIAL sandbox app holding media.object.upload (and media.object.read for --get), audience openvibe.media.
OV_NETWORK_URL=https://openvibe.network
OV_CLIENT_ID=
OV_CLIENT_SECRET=
# Optional: your project id (prj_...), the Media tenant. Read from the app token when empty.
OV_PROJECT_ID=
# Optional: an explicit Media origin instead of the one the registry advertises.
OV_MEDIA_URL=
