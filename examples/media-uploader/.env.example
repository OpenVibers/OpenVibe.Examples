# A CONFIDENTIAL developer app holding media.object.upload (audience openvibe.media).
OV_NETWORK_URL=https://openvibe.network
OV_CLIENT_ID=
OV_CLIENT_SECRET=
# Your project id (prj_...): Network puts it in the token's ns claim, and Media stores under it.
OV_MEDIA_NAMESPACE=
# Optional: an explicit Media origin instead of the one the registry advertises.
OV_MEDIA_URL=
