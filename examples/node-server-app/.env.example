# A CONFIDENTIAL developer app in your OpenVibe project (client id app_..., secret ovsec_... shown once).
OV_NETWORK_URL=https://openvibe.network
OV_CLIENT_ID=
OV_CLIENT_SECRET=
# Audiences to ask for app tokens (comma-separated); /status shows the capabilities each token carries.
OV_AUDIENCES=openvibe.media,openvibe.events,openvibe.tools
OV_PORT=3002
